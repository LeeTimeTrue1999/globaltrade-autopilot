# 待决策问题

以下问题不阻塞 MVP 原型开发，先按默认值占位。

## 市场优先级

TODO_DECISION:

- 首发国家是否按 `泰国 -> 印尼 -> 越南 -> 菲律宾 -> 俄罗斯` 排序？
- 俄罗斯是否第一阶段只做机会分析，不做订单履约？

当前默认：

- 东南亚和俄罗斯都进入机会池。
- 俄罗斯方向提高风险分，不自动发布。

## 平台账号

TODO_DECISION:

- 是否已经拥有 Shopee、TikTok Shop、Ozon 卖家账号？
- 是否具备开放平台 API 权限？

当前默认：

- MVP 先通过手动导入/导出。
- API 接入放在第二阶段。

## 供应链来源

TODO_DECISION:

- 是否已经有稳定 1688 供应商或自有工厂？
- 是否接受一件代发作为首发履约方式？

当前默认：

- 1688 + 手动供应商库。
- 首发按小单测试，不做大批量备货。

## 风险偏好

TODO_DECISION:

- 最低毛利率阈值是多少？
- 单品首批测试订单数是多少？
- 是否允许自动调价？

当前默认：

- 最低毛利率 25%。
- 首批测试 5-20 单。
- 自动调价只生成建议，不直接执行。

## 技术栈

TODO_DECISION:

- 后端优先 Python/FastAPI 还是 Node/NestJS？
- 前端是否使用 Next.js？
- 数据库是否使用 PostgreSQL？

当前默认：

- 原型无外部依赖。
- 正式 MVP 推荐 Next.js + FastAPI + PostgreSQL。

